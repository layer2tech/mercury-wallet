{
  "name": "wallet-app",
  "version": "0.1.0",
  "main": "main.js",
  "scripts": {
    "build": "electron-build-env neon build client --release",
    "start": "electron .",
    "rebuild": "yarn upgrade && yarn build"
  },
  "devDependencies": {
    "electron-build-env": "^0.2.0",
    "eslint": "^6.8.0",
    "neon-cli": "^0.3.3"
  },
  "dependencies": {
    "client": "file:client",
    "electron": "^7.1.11"
  },
  "eslintConfig": {
    "rules": {
      "import/no-unresolved": "off"
    }
  }
}
