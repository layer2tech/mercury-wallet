{
  "name": "wallet-app",
  "version": "0.1.0",
  "main": "main.js",
  "scripts": {
    "build": "electron-build-env neon build neon-lib --release",
    "install": "yarn build",
    "start": "electron ."
  },
  "devDependencies": {
    "electron-build-env": "^0.2.0",
    "eslint": "^6.8.0",
    "neon-cli": "^0.3.3"
  },
  "dependencies": {
    "electron": "^7.1.11",
    "neon-lib": "file:neon-lib"
  },
  "eslintConfig": {
    "rules": {
      "import/no-unresolved": "off"
    }
  }
}
