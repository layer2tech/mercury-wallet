// import * as fs from 'fs';
// // import { initializeWasmFromBinary } from 'lightningdevkit';

import * as wasm from 'client-wasm';

describe('LDK',() => {
    test('module load', async ()=> {
        
        // const wasm_file = fs.readFileSync('node_modules/lightningdevkit/liblightningjs.wasm');

        // await initializeWasmFromBinary(wasm_file);
       
        
        
    })
})